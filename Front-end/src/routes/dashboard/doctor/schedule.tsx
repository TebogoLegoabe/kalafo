import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/dashboard/doctor/schedule')({
  component: RouteComponent,
})

function RouteComponent() {
  return <div>
    <title>Doctor's Schedule - Kalafo</title>
    schedules</div>
}
